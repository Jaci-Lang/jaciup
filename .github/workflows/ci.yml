name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  test:
    name: test-${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-latest ]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Download Jaci Engine Bootstrap (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          URL=$(curl -sL "https://pop.squareweb.app/v1/releases/latest?product=luau" | jq -r '.artifacts["x86_64-unknown-linux-gnu"].url')
          curl -fSL -o luau-x86_64-unknown-linux-gnu.zip "$URL"
          unzip -o -q luau-x86_64-unknown-linux-gnu.zip -d /tmp/jaci
          chmod +x /tmp/jaci/*
          sudo cp /tmp/jaci/luau /usr/local/bin/luau || sudo cp /tmp/jaci/luau* /usr/local/bin/luau || true

      - name: Download Jaci Engine Bootstrap (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          URL=$(curl -sL "https://pop.squareweb.app/v1/releases/latest?product=luau" | jq -r '.artifacts["x86_64-apple-darwin"].url')
          curl -fSL -o luau-x86_64-apple-darwin.zip "$URL"
          unzip -o -q luau-x86_64-apple-darwin.zip -d /tmp/jaci
          chmod +x /tmp/jaci/*
          sudo cp /tmp/jaci/luau /usr/local/bin/luau || cp /tmp/jaci/luau /usr/local/bin/luau || true

      - name: Download Jaci Engine Bootstrap (Windows)
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: |
          $json = Invoke-RestMethod -Uri "https://pop.squareweb.app/v1/releases/latest?product=luau"
          $url = $json.artifacts."x86_64-pc-windows-msvc".url
          Invoke-WebRequest -Uri $url -OutFile luau-x86_64-pc-windows-msvc.zip
          Expand-Archive -Path luau-x86_64-pc-windows-msvc.zip -DestinationPath C:\jaci -Force
          echo "C:\jaci" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Run Jaciup Test Suite
        shell: bash
        run: |
          chmod +x bin/jaciup
          ./bin/jaciup --help
          ./bin/jaciup show
